# Reference-only vendor checkout — a separate git clone of the upstream
# foundryvtt-pathfinder1 repo (its own .git/remote/history), kept around
# purely to read for reference. Not part of this project.
RefCode/

# OS cruft
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editors / IDEs
.vscode/
!.vscode/extensions.json
.idea/
.vs/
*.code-workspace
*.swp
*.swo
*~
*.orig
*.rej
.history/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm/
.eslintcache
*.tsbuildinfo

# Bundler / build output (none yet, but keep this clean if one is added)
dist/
build/
.vite/
.turbo/
.parcel-cache/

# Test / coverage output
coverage/
.nyc_output/

# Local env / secrets
.env
.env.local
.env.*.local
.envrc

# Foundry local-dev config (League of FoundryVTT Devs installer config, etc.)
foundryconfig.json

# Potential local symlink into a Foundry Data/ install for testing
/foundry

# Packaged release artifacts (e.g. a local "npm run pack" zip for testing
# before attaching it to a GitHub release)
*.zip

# Claude Code personal overrides (project-shared settings.json, if any,
# should still be committed)
.claude/settings.local.json

# AI planning / working notes — the actual plan lives outside the repo
# (see ~/.claude/plans/); nothing under these names should be checked in
.claude/plans/
PLAN.md
*.plan.md
TODO-ai.md
.ai/

# Local dev reference docs — not project deliverables
/Docs/

# Misc
*.tmp
*.log
!elyndor-house-rules/packs/**/*.log
